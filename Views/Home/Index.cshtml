@{
    ViewData["Title"] = "Chat & Video Call";
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewData["Title"]</title>
    <link rel="stylesheet" href="~/css/style.css" />
    <script src="https://cdnjs.cloudflare.com/ajax/libs/microsoft-signalr/8.0.0/signalr.min.js"></script>
</head>
<body>
    <!-- Username Entry Popup -->
    <div class="username-popup" id="usernamePopup">
        <h2>🎉 Welcome!</h2>
        <p>Enter your name to get started</p>
        <input type="text" id="displayname" placeholder="Your name" autofocus />
        <br><br>
        <button id="joinBtn">Join Now</button>
    </div>

    <!-- Incoming Call Popup -->
    <div class="incoming-call-popup" id="incomingCallPopup">
        <h2>📞 Incoming Call</h2>
        <div class="caller-name" id="callerName"></div>
        <div class="call-info" id="callMediaInfo"></div>
        <div class="popup-buttons">
            <button class="accept-btn" id="acceptCallPopup">✅ Accept</button>
            <button class="reject-btn" id="rejectCallPopup">❌ Decline</button>
        </div>
    </div>

    <!-- Main Application -->
    <div class="container">
        <!-- Sidebar -->
        <aside class="sidebar">
            <h3>👥 Active Users</h3>
            <ul id="activeUsersList"></ul>
        </aside>

        <!-- Main Content -->
        <main class="main">
            <!-- Call Controls -->
            <div class="call-controls">
                <button id="endCall">📞 End Call</button>
                <button id="toggleVideo">📹 Video On</button>
                <button id="toggleAudio">🎤 Mic On</button>
            </div>

            <!-- Video Streams (hidden by default, shown during call) -->
            <div class="video-container" style="display: none;">
                <div class="video-box">
                    <video id="localVideo" autoplay muted playsinline></video>
                    <span class="video-label">You</span>
                </div>
                <div class="video-box">
                    <video id="remoteVideo" autoplay playsinline></video>
                    <span class="video-label">Remote</span>
                </div>
            </div>

            <!-- Chat -->
            <div class="chat-container">
                <h3>💬 Messages</h3>
                <div id="chatMessages"></div>
                <div class="message-input-container">
                    <input type="text" id="message" placeholder="Select a user, then type a message..." />
                    <button id="sendmessage">Send</button>
                </div>
            </div>
        </main>
    </div>

    <script src="~/js/app.js"></script>
</body>
</html>
